from __future__ import annotations


# ============================================================
# ИДЕЯ
# ============================================================
#
# Три разные версии, и путать их нельзя.
#
# FORMAT_VERSION — контракт примера: состав массивов, смысл
# смещений событий и правило разбора значений по positions. Его
# читает будущий Masker и будущая модель, и его смена означает,
# что прежние наборы им больше не подходят. Контракт батча живёт
# отдельно, в src/batching/version.py: выравнивание это другой
# этап, и версии у них разные.
#
# IMPLEMENTATION_VERSION — версия кода пакета. Поднимается при
# ЛЮБОЙ правке модуля, даже если формат не тронут: иначе
# прежний набор считался бы собранным этим кодом.
#
# SCHEMA_VERSION — схема файлов на диске.
# ============================================================


FORMAT_VERSION = "6.0.0"

IMPLEMENTATION_VERSION = "6.1.0"

SCHEMA_VERSION = 3


__all__ = ["FORMAT_VERSION", "IMPLEMENTATION_VERSION", "SCHEMA_VERSION"]
