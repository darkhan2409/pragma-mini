from __future__ import annotations


# ============================================================
# ВЕРСИИ
# ============================================================
#
# FORMAT_VERSION — контракт энкодера события: внимание внутри
# события, вектор события из позиции [EVT], календарь слагаемым
# после энкодера. Его читает будущий History Encoder.
#
# IMPLEMENTATION_VERSION — версия кода пакета. Поднимается при
# ЛЮБОЙ правке модуля, даже если контракт не тронут.
#
# SCHEMA_VERSION — схема events.parquet. Версия 2: строка стала
# одним событием вместо одного клиента, вектор остался один —
# итоговый, после календаря.
#
# Число слоёв, голов и размер порции сюда не входят: это решение
# человека, и живёт оно в конфигурации.
# ============================================================


FORMAT_VERSION = "2.0.0"

IMPLEMENTATION_VERSION = "2.3.0"

SCHEMA_VERSION = 2


__all__ = ["FORMAT_VERSION", "IMPLEMENTATION_VERSION", "SCHEMA_VERSION"]
