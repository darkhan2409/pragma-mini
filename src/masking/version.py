from __future__ import annotations


# ============================================================
# ВЕРСИИ
# ============================================================
#
# Три разные версии, и путать их нельзя.
#
# FORMAT_VERSION — контракт маскирования: состав колонок, смысл
# -100 в labels, набор причин и правило «все куски значения
# получают одно решение». Его читает будущая модель.
#
# IMPLEMENTATION_VERSION — версия кода пакета. Поднимается при
# ЛЮБОЙ правке модуля, даже если формат не тронут.
#
# SCHEMA_VERSION — схема файла на диске.
#
# Вероятности и seed сюда не входят: они решение человека и
# живут в конфигурации, а не в версии формата.
# ============================================================


FORMAT_VERSION = "1.0.0"

IMPLEMENTATION_VERSION = "1.2.0"

SCHEMA_VERSION = 1


__all__ = ["FORMAT_VERSION", "IMPLEMENTATION_VERSION", "SCHEMA_VERSION"]
